<UserControl x:Class="InvoiceDesk.Wpf.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InvoiceDesk.Wpf.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:PlaceholderViewModel}"
             mc:Ignorable="d">

    <Border Style="{StaticResource CardBorder}" Padding="28">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="420">
            <TextBlock FontFamily="{StaticResource IconFont}" FontSize="28" HorizontalAlignment="Center"
                       Foreground="{DynamicResource TextMutedBrush}" Text="{Binding Icon}" />
            <TextBlock Style="{StaticResource PageTitleText}" Margin="0,14,0,0" HorizontalAlignment="Center"
                       Text="{Binding Title}" />
            <TextBlock Margin="0,8,0,0" FontSize="13" TextAlignment="Center" TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding Description}" />
            <TextBlock Style="{StaticResource CaptionText}" Margin="0,16,0,0" HorizontalAlignment="Center"
                       Text="Planned for an upcoming milestone" />
        </StackPanel>
    </Border>
</UserControl>
